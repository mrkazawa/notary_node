{
  "name": "storage",
  "version": "1.0.0",
  "description": "Storage Engine: IPFS Implementation",
  "main": "ipfs_engine.js",
  "scripts": {
    "build": "chmod +x ./build_ipfs.sh && ./build_ipfs.sh",
    "distribute-keys": "chmod +x ./distribute_keys_ipfs.sh && ./distribute_keys_ipfs.sh true",
    "add-boot-node": "chmod +x ./add_boot_ipfs.sh && ./add_boot_ipfs.sh",
    "start": "chmod +x ./start_ipfs.sh && ./start_ipfs.sh",
    "stop": "chmod +x ./stop_ipfs.sh && ./stop_ipfs.sh",
    "destroy": "chmod +x ./destroy_ipfs.sh && ./destroy_ipfs.sh",
    "test": "jest"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mrkazawa/notary_node.git"
  },
  "keywords": [
    "storage-engine",
    "ipfs"
  ],
  "author": "Yustus Oktian",
  "license": "ISC",
  "dependencies": {
    "ipfs-http-client": "^40.1.0",
    "is-ipfs": "^0.6.1"
  },
  "devDependencies": {
    "jest": "^25.4.0"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
